package com.hridayacreations.entity.enums;

/**
 * Application roles used for Role-Based Access Control (RBAC).
 * The {@code ROLE_} prefix follows Spring Security's authority convention.
 */
public enum RoleName {
    ROLE_ADMIN,
    ROLE_USER
}
